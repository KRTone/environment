# Запуск проекта

Пошаговая инструкция развёртывания среды с нуля.

Связанные разделы: [[Установка компонентов]] · [[Схема проекта]] · [[Справочник и устранение неполадок]]

---

## Обзор

```
terraform/   →  сеть + Gitea + act_runner + Docker registry
Браузер      →  первый вход, смена пароля, создание репозитория
```

---

## Развернуть всё

Перед apply включите **Kubernetes в Docker Desktop**. Репозиторий .NET создаётся вручную из `terraform/templates/dotnet/` — см. [[.NET CI-CD]].

```powershell
cd terraform
terraform init
terraform apply
```

Terraform создаст:

- Docker-сеть `environment-network`
- Gitea с учётной записью администратора
- act_runner с автоматической регистрацией
- Docker registry (`environment-registry`, порт `30500`) для CI

Подтвердите: введите `yes`.

### (Опционально) Настройки

```powershell
copy terraform.tfvars.example terraform.tfvars
```

Пример `terraform.tfvars`:

```hcl
admin_password = "надёжный_пароль"
admin_email    = "you@example.com"
```

Затем снова `terraform apply`.

### Проверка

```powershell
docker ps --filter name=gitea
docker ps --filter name=gitea-act-runner
docker ps --filter name=environment-registry
terraform output
```

Откройте **http://localhost:3000**, войдите как `admin` / `admin` (или ваш пароль из `tfvars`).

В Gitea: **Site Administration → Actions → Runners** — runner должен быть **Online**.

---

## Первичная настройка для разработки

### Сменить пароль администратора

**Settings → Account → Change Password**

### Создать репозиторий

```powershell
# HTTPS
git clone http://localhost:3000/admin/my-repo.git

# SSH (порт 2222)
git clone ssh://git@localhost:2222/admin/my-repo.git
```

### Пример workflow

`.gitea/workflows/ci.yml`:

```yaml
name: CI
on: [push]
jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - run: echo "Hello from Gitea Actions"
```

---

## Остановка и удаление

```powershell
cd terraform
terraform destroy
```

> [!caution] Данные
> Тома `gitea-data` и `gitea-runner-data` сохраняются в Docker после `destroy`. Полная очистка: `docker volume rm gitea-data gitea-runner-data`.

---

## Полезные команды

| Действие | Команда |
|----------|---------|
| Логи Gitea | `docker logs -f gitea` |
| Логи runner | `docker logs -f gitea-act-runner` |
| Healthcheck Gitea | `docker inspect --format='{{.State.Health.Status}}' gitea` |
| Outputs | `cd terraform; terraform output` |
| Токен runner | `terraform output -raw runner_registration_token` |

Подробнее — в [[Справочник и устранение неполадок]].

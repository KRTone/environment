# Справочник и устранение неполадок

Сводка переменных, outputs и типичных проблем.

Связанные разделы: [[Главная]] · [[Запуск проекта]] · [[Схема проекта]]

---

## Outputs корневого стека (`terraform/`)

| Output | Описание |
|--------|----------|
| `network_name` | Имя Docker-сети |
| `gitea_url` | URL веб-интерфейса |
| `gitea_ssh_port` | SSH-порт |
| `admin_username` / `admin_email` | Учётная запись администратора |
| `gitea_container_name` | Имя контейнера Gitea |
| `runner_container_name` | Имя контейнера runner |
| `runner_name` | Имя runner в UI |
| `runner_registration_token` | Токен runner (**sensitive**) |
| `registry_url` | URL локального Docker registry для CI |
| `registry_container_name` | Имя контейнера registry |

```powershell
cd terraform
terraform output
```

---

## Переменные корневого стека

| Переменная | По умолчанию | Описание |
|------------|--------------|----------|
| `gitea_image` | `gitea/gitea:latest` | Образ Gitea |
| `gitea_port` | `3000` | HTTP-порт |
| `gitea_ssh_port` | `2222` | SSH-порт |
| `gitea_root_url` | `http://gitea:3000` | URL Gitea в Docker-сети |
| `admin_username` | `admin` | Логин админа |
| `admin_password` | `admin` | Пароль админа |
| `admin_email` | `admin@admin.admin` | Email админа |
| `act_runner_image` | `gitea-act-runner:local` | Образ runner |
| `runner_name` | `gitea-docker-runner` | Имя runner |
| `runner_labels` | `self-hosted,linux,docker` | Метки |
| `runner_container_name` | `gitea-act-runner` | Имя контейнера runner |
| `docker_socket_path` | `/var/run/docker.sock` | Docker socket |
| `registry_container_name` | `docker-registry` | DNS-имя registry в сети |
| `registry_address` | `docker-registry:5000` | Адрес registry (output) |
| `create_k8s_cluster` | `true` | k3d-кластер через Terraform |
| `k8s_cluster_name` | `local` | Имя k3d-кластера |
| `kubeconfig_host_path` | `.generated/kubeconfig-local` | Путь kubeconfig для act_runner |

---

## Типичные проблемы

### Docker не запущен

**Симптом:** `error during connect` / `Cannot connect to the Docker daemon`

**Решение:** запустите Docker Desktop, дождитесь статуса Running, повторите `terraform apply`.

---

### k3d не найден в PATH

**Симптом:** `Error: local-exec provisioner error` / `k3d не найден в PATH` при создании `module.k8s_cluster`

**Причина:** утилита k3d не установлена или недоступна в PATH среды, из которой запускается Terraform (часто IDE vs отдельный PowerShell).

**Решение:**

1. Установите k3d — см. [[Установка компонентов#6. k3d]]
2. Проверьте: `k3d version`
3. Перезапустите терминал или IDE
4. Повторите `terraform apply`

Либо отключите k3d в `terraform.tfvars`: `create_k8s_cluster = false` (нужен другой kubeconfig).

---

### Порт 3000 или 2222 занят

**Симптом:** `port is already allocated`

**Решение:** освободите порт или измените в `terraform.tfvars`:

```hcl
gitea_port     = 3001
gitea_ssh_port = 2223
gitea_root_url = "http://gitea:3001"
gitea_port     = 3001
```

Затем `terraform apply`.

---

### Gitea container is not healthy

**Симптом:** ошибка provisioner `gitea_admin` при `terraform apply`

**Решение:**

1. `docker logs gitea` — проверьте ошибки старта
2. `docker inspect --format='{{.State.Health.Status}}' gitea`
3. Убедитесь, что порт 3000 доступен внутри контейнера
4. При повторном apply provisioner пропустит создание, если пользователь уже существует

---

### Runner offline в Gitea

**Симптом:** в **Actions → Runners** runner не появляется или Offline

**Проверки:**

1. `docker logs gitea-act-runner`
2. Сеть `gitea-network` должна существовать (`docker network ls`) — создаётся в `terraform/network.tf`
3. Gitea доступен: `docker exec gitea-act-runner wget -q -O- http://gitea:3000/api/healthz`

---

### kubectl: error reading [/k8s/] — recognized file extensions

**Симптом:** `error reading [/k8s/]: recognized file extensions are [.json .yaml .yml]` при deploy

**Причина:** nested `docker run -v $GITHUB_WORKSPACE/k8s:/k8s` монтирует путь с **хоста**, а не из job-контейнера — каталог `/k8s` в kubectl-контейнере пуст.

**Решение:** манифесты передаются в `kubectl apply -f -` через stdin; kubeconfig монтируется с хоста (`KUBECONFIG_HOST`) в nested `docker run`.

---

### kubectl: дефолтный API недоступен

**Симптом:** `connection refused` при deploy

**Причина:** `/kube/config` не смонтирован в job-контейнер или kubeconfig не указывает на `k3d-local-server-0:6443`.

**Решение:** выполните `terraform apply`, проверьте mount kubeconfig в `act_runner/config.yaml.tpl`, перезапустите runner.

---

### Duplicate mount point: /var/run/docker.sock

**Симптом:** `failed to create container: Duplicate mount point: /var/run/docker.sock` на шаге **Set up job**

**Причина:** в `act_runner/config.yaml.tpl` docker.sock был указан и в `options: -v ...`, и в `valid_volumes` — act_runner монтировал socket дважды.

**Решение:** оставить только `valid_volumes`. Выполните `terraform apply` и `docker restart gitea-act-runner`.

---

### Cannot find: node in PATH (actions/checkout)

**Симптом:** `Cannot find: node in PATH` на шаге Checkout

**Причина:** `actions/checkout` и `actions/setup-dotnet` — JavaScript-actions, им нужен Node.js в runner. act_runner его не предоставляет по умолчанию.

**Решение:** workflow использует `git clone` через shell и образы `mcr.microsoft.com/dotnet/sdk` / `docker:24-cli` вместо JS-actions. Выполните `terraform apply` для обновления репозитория, затем перезапустите workflow.

---

### actions/checkout: connection refused к Gitea

**Симптом:** `Unable to clone http://gitea:3000/actions/checkout` / connection refused

**Причина:** job-контейнер не в `gitea-network` или неверный URL Gitea.

**Решение:** в Terraform задано:
- `GITEA__actions__DEFAULT_ACTIONS_URL=http://gitea:3000` (внутренний URL)
- job-контейнеры подключаются к `gitea-network` (`act_runner/config.yaml.tpl`)

Выполните `terraform apply` для пересоздания Gitea и act_runner, затем перезапустите workflow.

Альтернатива (нужен интернет): `gitea_actions_url = "github"` в `terraform.tfvars`.

---

### Job зависает или не стартует

**Симптом:** workflow в очереди, нет логов

**Проверки:**

1. В `.gitea/workflows/*.yml` указано `runs-on: self-hosted` (или метка из `runner_labels`)
2. Actions включены в настройках репозитория
3. Runner Online
4. Docker socket доступен runner'у (`docker logs gitea-act-runner`)

---

### Ошибка сети при apply act_runner

**Симптом:** `network gitea-network not found`

**Решение:** выполните `terraform init` и `terraform apply` в каталоге `terraform/`.

---

## Обновление образов

```powershell
cd terraform
terraform apply
```

Или вручную подтяните образы и снова `terraform apply`:

```powershell
docker pull gitea/gitea:latest
docker pull registry:2
```

---

## Безопасность (минимум для dev-среды)

- Смените пароль `admin` после первого входа
- Не коммитьте `terraform.tfvars` с токенами и паролями
- Токен `runner_registration_token` даёт регистрацию runner — храните локально
- Среда по умолчанию рассчитана на **локальную разработку**, не на публичный интернет без дополнительной настройки (TLS, файрвол, сильные пароли)

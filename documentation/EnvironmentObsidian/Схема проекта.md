# Схема проекта

Архитектура локальной среды: компоненты, сети, тома и потоки данных.

Связанные разделы: [[Главная]] · [[Запуск проекта]] · [[Установка компонентов]]

---

## Общая схема (одна машина)

Типичный сценарий: Gitea и act_runner на одном хосте с Docker Desktop.

```mermaid
flowchart TB
    subgraph Host["Хост разработчика (Windows)"]
        subgraph Docker["Docker Engine"]
            subgraph Net["Docker-сеть: gitea-network"]
                Gitea["Контейнер: gitea<br/>gitea/gitea:latest<br/>:3000 HTTP, :22 SSH"]
                Runner["Контейнер: gitea-act-runner<br/>gitea/act_runner:latest"]
            end
            VolGitea[("Том: gitea-data<br/>БД SQLite, репозитории")]
            VolRunner[("Том: gitea-runner-data<br/>Состояние runner")]
            Sock["/var/run/docker.sock"]
        end
        TF_Root["Terraform<br/>terraform/"]
        Dev["Разработчик<br/>Git · Браузер · IDE"]
    end

    TF_Root -->|network.tf| Net
    TF_Root -->|модуль gitea| Gitea
    TF_Root -->|модуль gitea| VolGitea
    TF_Root -->|модуль act_runner| Runner
    TF_Root -->|модуль act_runner| VolRunner

    Gitea --- VolGitea
    Runner --- VolRunner
    Runner -->|монтирует| Sock
    Runner -->|"GITEA_INSTANCE_URL<br/>http://gitea:3000"| Gitea

    Dev -->|"http://gitea:3000"| Gitea
    Dev -->|"ssh://git@gitea:2222"| Gitea
    Dev -->|git push| Gitea
    Gitea -->|ставит job в очередь| Runner
    Runner -->|запускает job-контейнеры| Sock
```

---

## Корневой Terraform-стек

Рекомендуемая схема: один state в `terraform/`, сеть на верхнем уровне, модули подключаются из подкаталогов.

```mermaid
flowchart LR
    subgraph Root["terraform/ (корневой стек)"]
        N[docker_network<br/>gitea-network<br/>network.tf]
        subgraph ModGitea["модуль ./gitea"]
            R1[random_password<br/>runner_token]
            VG[docker_volume<br/>gitea-data]
            C1[docker_container<br/>gitea]
            B[docker_container<br/>gitea_admin_bootstrap]
            R1 --> C1
            VG --> C1
            C1 --> B
        end
        subgraph ModRunner["модуль ./act_runner"]
            VR[docker_volume<br/>gitea-runner-data]
            BI[docker_image<br/>act_runner build]
            C2[docker_container<br/>gitea-act-runner]
            VR --> C2
            BI --> C2
        end
        subgraph ModRegistry["модуль ./docker_registry"]
            C3[docker_container<br/>docker-registry:5000]
        end
        subgraph ModK8s["модуль ./k8s_cluster"]
            K3D[k3d_cluster<br/>SneakyBugs/k3d]
        end
        subgraph ModArgo["модуль ./argocd"]
            C5[Helm + argocd_*<br/>argoproj-labs/argocd]
        end
        N --> C1
        N --> C2
        N --> C3
        N --> C4
        ModGitea -->|"runner_registration_token"| ModRunner
        ModK8s --> ModArgo
    end
```

| Уровень | Каталог / файл | Создаёт |
|---------|----------------|---------|
| Корень | `terraform/network.tf` | Docker-сеть `gitea-network` |
| Модуль | `terraform/gitea/` | Том, контейнер Gitea, bootstrap admin (CLI в Docker) |
| Модуль | `terraform/act_runner/` | Том runner, образ (build), контейнер act_runner |
| Модуль | `terraform/docker_registry/` | Registry `docker-registry:5000` в сети |
| Модуль | `terraform/k8s_cluster/` | `k3d_cluster` (провайдер SneakyBugs/k3d) |
| Модуль | `terraform/argocd/` | Helm + `argocd_repository` + `argocd_application` |

Сеть `gitea-network` задаётся в `network.tf` и не настраивается через переменные.

---

## Сетевая модель

### Сетевая связность

| Откуда | Куда | Адрес | Назначение |
|--------|------|-------|------------|
| Браузер / Git (из сети) | Gitea | `gitea:3000`, `gitea:2222` | UI и SSH из gitea-network |
| act_runner | Gitea | `http://gitea:3000` | API внутри `gitea-network` |
| Gitea | Runner | `gitea-network` | Постановка job'ов Actions |

---

## Поток CI/CD (GitOps)

```mermaid
sequenceDiagram
    participant Dev as Разработчик
    participant Gitea as Gitea
    participant Runner as act_runner
    participant Job as Job-контейнер
    participant ArgoCD as ArgoCD
    participant K8s as k3d

    Dev->>Gitea: git push (код)
    Gitea->>Runner: workflow CI
    Runner->>Job: test + build + push image
    Job->>Gitea: git push (newTag в overlay)
    ArgoCD->>Gitea: poll Git
    ArgoCD->>K8s: sync
```

**Метки runner по умолчанию:** `self-hosted`, `linux`, `docker`  
В workflow указывайте `runs-on: self-hosted` (или другую метку из `runner_labels`).

---

## Хранение данных

| Том Docker | Содержимое |
|------------|------------|
| `gitea-data` | SQLite БД (`/data/gitea/gitea.db`), git-репозитории, артефакты, настройки |
| `gitea-runner-data` | Регистрация и рабочее состояние act_runner |

База данных: **SQLite** (файл внутри тома). Для продакшена в других сценариях часто используют PostgreSQL — в текущем Terraform зашит SQLite.

---

## Порты и переменные окружения Gitea

Ключевые настройки контейнера `gitea` (из `terraform/gitea/main.tf`):

| Переменная / параметр | Значение по умолчанию |
|-----------------------|----------------------|
| `GITEA__server__HTTP_PORT` | 3000 |
| `GITEA__server__SSH_PORT` | 2222 |
| `GITEA__server__ROOT_URL` | http://gitea:3000 |
| `GITEA__actions__ENABLED` | true |
| `GITEA_RUNNER_REGISTRATION_TOKEN` | генерируется Terraform |

---

## Зависимости между компонентами

```mermaid
flowchart TD
    DockerDesktop[Docker Desktop] --> Terraform
    Terraform --> RootApply[terraform apply]
    RootApply --> GiteaRunning[Gitea healthy + admin]
    GiteaRunning --> RunnerOnline[Runner + registry]
    RunnerOnline --> Workflows[Workflows в репозиториях]

    PowerShell --> RootApply
```

Один `terraform apply` в каталоге `terraform/` поднимает всю среду. Без работающего Docker apply не выполнится. Без healthcheck Gitea provisioner `gitea_admin` завершится с ошибкой.

---

## Файловая структура Terraform

```
terraform/
├── main.tf              # модули gitea + act_runner + docker_registry + k8s_cluster
├── network.tf           # Docker-сеть gitea-network
├── variables.tf
├── outputs.tf
├── terraform.tfvars.example
├── providers.tf
├── versions.tf
├── docker_registry/
│   ├── main.tf
│   ├── variables.tf
│   └── outputs.tf
├── k8s_cluster/         # модуль k3d
│   ├── main.tf
│   ├── variables.tf
│   └── outputs.tf
├── argocd/              # Helm + Application CRD
│   ├── main.tf
│   ├── variables.tf
│   ├── outputs.tf
│   └── values/
├── gitea/
│   ├── main.tf
│   ├── variables.tf
│   └── outputs.tf
└── act_runner/
    ├── main.tf
    ├── variables.tf
    ├── outputs.tf
    ├── config.yaml.tpl
    └── Dockerfile
```

Далее: [[Запуск проекта]]
